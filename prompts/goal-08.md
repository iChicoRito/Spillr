Objective: Keep the Deck question generation UI as a prototype-only flow

1. Preserve the current question generation dialog and review flow in the UI.

2. Remove any runtime dependency on local AI or on-device model files.

3. Keep the generated question behavior deterministic and grounded in the selected deck category so the prototype still demos cleanly.
