Objective: Change the current AI generation for Decks

1. The current AI installed on the app is ML Kit GenAI / Gemini Nano / AICore

2. Change the current AI to Gemma 3 270M (flutter_gemma)

3. Update the UI of the current generation of question using AI. The design I made in figma should be followed stricly. Only the dialog part because the sheet for create is fine and no changes.

4. Make sure that the AI can work fully offline and same functionalities that it only generates question based on the selected deck category, not random.