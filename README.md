# Spillr

A Flutter party-card app with a prototype question generation flow for decks.

The current app keeps the generate/review UI in place for demo purposes, but it
does not rely on a local AI model at runtime.
